{
  "name": "min-cap",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "scripts": {
    "build": "mkdir -p out && cp -r src/app out/app",
    "serve": "serve out/app",
    "electron": "electron src/electron/main.js"
  },
  "dependencies": {
    "electron": "^40.1.0",
    "serve": "^14.2.5"
  }
}
